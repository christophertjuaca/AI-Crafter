import { Tone, CompanyType, Language } from './types';

export const TONE_OPTIONS: Tone[] = Object.values(Tone);
export const COMPANY_TYPE_OPTIONS: CompanyType[] = Object.values(CompanyType);
export const LANGUAGE_OPTIONS: Language[] = Object.values(Language);